// project import
import dashboard from './dashboard';
import entities from './entities';
import exceptions from './exceptions';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, exceptions, entities]
};

export default menuItems;
